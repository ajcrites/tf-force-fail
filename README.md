This is a sample repo created to showcase Terraform v 0.7
failing when `terraform` is called during `local-exec`
provisioning.

Run `terraform apply` to reproduce [the issue](https://github.com/hashicorp/terraform/issues/7955).
